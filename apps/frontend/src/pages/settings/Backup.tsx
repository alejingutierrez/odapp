import React from 'react'
import { Typography, Card } from 'antd'

const Backup: React.FC = () => {
  return (
    <div>
      <Typography.Title level={2}>Backup Settings</Typography.Title>
      <Card>
        <p>Backup Settings page will be implemented here...</p>
      </Card>
    </div>
  )
}

export default Backup
